<Window x:Class="Lab3Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:conv="clr-namespace:Lab3Wpf.Converters"
        mc:Ignorable="d"
        Title="Лабораторная работа 3" Height="720" Width="1100"
        MinHeight="620" MinWidth="900">

    <Window.Resources>
        <conv:GridValuesConverter x:Key="GridValuesConverter" />
        <conv:PlotModelConverter x:Key="PlotModelConverter" />

        <Style TargetType="TextBox" x:Key="ValidationTextBoxStyle">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                    <Setter Property="BorderBrush" Value="Red" />
                    <Setter Property="BorderThickness" Value="2" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="8" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="120" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="ItemsGrid"
                  Grid.Row="0"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  ColumnWidth="*"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    EditingElementStyle="{StaticResource ValidationTextBoxStyle}">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Name"
                                 Mode="TwoWay"
                                 UpdateSourceTrigger="PropertyChanged"
                                 ValidatesOnDataErrors="True"
                                 ValidatesOnExceptions="True"
                                 NotifyOnValidationError="True" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Date"
                                    Binding="{Binding Date, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=dd.MM.yyyy HH:mm:ss, ValidatesOnExceptions=True}" />

                <DataGridTextColumn Header="N">
                    <DataGridTextColumn.Binding>
                        <Binding Path="N"
                                 Mode="TwoWay"
                                 UpdateSourceTrigger="PropertyChanged"
                                 ValidatesOnExceptions="True" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

                <DataGridTextColumn Header="LeftBound"
                                    EditingElementStyle="{StaticResource ValidationTextBoxStyle}">
                    <DataGridTextColumn.Binding>
                        <Binding Path="LeftBound"
                                 Mode="TwoWay"
                                 UpdateSourceTrigger="PropertyChanged"
                                 ValidatesOnDataErrors="True"
                                 ValidatesOnExceptions="True"
                                 NotifyOnValidationError="True" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

                <DataGridTextColumn Header="RightBound"
                                    EditingElementStyle="{StaticResource ValidationTextBoxStyle}">
                    <DataGridTextColumn.Binding>
                        <Binding Path="RightBound"
                                 Mode="TwoWay"
                                 UpdateSourceTrigger="PropertyChanged"
                                 ValidatesOnDataErrors="True"
                                 ValidatesOnExceptions="True"
                                 NotifyOnValidationError="True" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="Узлы сетки и значения функции" Grid.Column="0">
                <ListBox FontFamily="Consolas">
                    <ListBox.ItemsSource>
                        <MultiBinding Converter="{StaticResource GridValuesConverter}">
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.LeftBound" />
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.RightBound" />
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.N" />
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.Values" />
                        </MultiBinding>
                    </ListBox.ItemsSource>
                </ListBox>
            </GroupBox>

            <GroupBox Header="График" Grid.Column="2">
                <oxy:PlotView>
                    <oxy:PlotView.Model>
                        <MultiBinding Converter="{StaticResource PlotModelConverter}">
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.LeftBound" />
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.RightBound" />
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.N" />
                            <Binding ElementName="ItemsGrid" Path="SelectedItem.Values" />
                        </MultiBinding>
                    </oxy:PlotView.Model>
                </oxy:PlotView>
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="Update"
                    Width="140"
                    Margin="0,0,8,0"
                    Padding="10,6"
                    Click="Update_Click" />
            <Button Content="Update Element"
                    Width="160"
                    Margin="0,0,8,0"
                    Padding="10,6"
                    Click="UpdateElement_Click" />
            <Button Content="Output"
                    Width="140"
                    Padding="10,6"
                    Click="Output_Click" />
        </StackPanel>

        <TextBox Grid.Row="6"
                 Text="{Binding Output}"
                 FontFamily="Consolas"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 TextWrapping="NoWrap" />
    </Grid>
</Window>
